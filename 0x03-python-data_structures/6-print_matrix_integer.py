#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for a in matrix:
        length = 1
        for b in a:
            if length == len(i):
                print("{:d}".format(b), end="")
            else:
                print("{:d}".format(b), end=" ")
            length = length + 1
        print()
